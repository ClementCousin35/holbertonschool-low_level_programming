#include "main.h"
#include <stdio.h>

/**
 * print_rev- prints a string to stdout
 * @s: pointer to the string to print
 */
void print_rev(char *str)
{
	int tmp;
	int i = 0;
	int l = 0;

	while(str[end] != '\0')
	{
	end++;
	}
	while(start < end);
	{
	tmp = str[start];
	str[start] = str[end];
_putchar('\n');
}
