#include "main.h"
#include <stdio.h>

/**
* _strlen- string et retour de s
* @s: pour compter le nombre de caractere
* Return: Always 0 (Success)
*/

int _strlen(char *s)
{
	char *str;

	str = 0;
	s = str;
	printf("%s\n", s);
return (*s);
}
