#include "main.h"

/**
*more_numbers- ecrire des chiffres en 10 fois
*Return: Always 0 (Success)
*/
void more_numbers(void)
{
	char a = (9);
	char b;
	char c;
	char e;
	char g = (a+b);

	for (c = '0'; c <= '9'; c++)
	{
		for (e = '0'; e <= '9'; e++)
		{
		_putchar(e);
		}
		{
		_putchar(g);
		}
	_putchar('\n');
	}
}
